# Your username to connect to Apigee for access to the proxy.
APIGEE_USERNAME=your-username

# Name of Apigee proxy to access the API through.
PROXY_NAME=immunisation-fhir-api-pr-number

# Apigee environment the proxy sits in.
APIGEE_ENVIRONMENT=internal-dev

# Used to find the proxy. Usually the same value as PROXY_NAME.
SERVICE_BASE_PATH=immunisation-fhir-api-pr-number

#This can be anything as it's only used for logs in the proxy.
SOURCE_COMMIT_ID=0000000000000000000000000000000000000000
